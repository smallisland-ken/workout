<% if @users.exists? %>
  <div class="container">
    <div class="row">
      <div class="col-sm-6 col-md7 col-lg-9 mt-3 mx-auto">
        <h2><i>Followers</i></h2>
        <div class="table-responsive">
          <table class="table mt-4"">
            <thead>
              <tr>
                <th class="text-center text-nowrap"><%= t 'favorites.followers.profile_image' %></th>
                <th class="text-center text-nowrap"><%= t 'favorites.followers.nickname' %></th>
                <th class="text-center text-nowrap"><%= t 'favorites.followers.follows' %></th>
                <th class="text-center text-nowrap"><%= t 'favorites.followers.followers' %></th>
              </tr>
          </thead>
          <tbody>
            <% @users.each do |user| %>
              <tr>
                <td class="text-center align-middle"><%= attachment_image_tag user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg", class:"mt-3 rounded-circle"; %></td>
                <td class="text-center align-middle"><%= user.nickname %></td>
                <td class="text-center align-middle"><%= user.followings.count %><%= t 'favorites.followers.following_people' %></td>
                <td class="text-center align-middle"><%= user.followers.count %><%= t 'favorites.followers.follower_people' %></td>
              </tr>
            <% end %>
          </tbody>
          </table>
          <% else %>
            <p>誰もフォローしていません。</p>
          <% end %>
        </div>
      </div>
  </div>
</div>